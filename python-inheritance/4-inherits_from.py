#!/usr/bin/python3
# 2-is_same_class.py
"""ref une fonction"""


def inherits_from(obj, a_class):

    """
    Checks if an object `

    Args:.

    Returns:
              (dir
    """

    if isinstance(obj, a_class) and type(obj) is not a_class:
        return True
    else:
        return False
